CREATE TABLE stocks_candle(
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    DAY DATE,
    P_OPEN NUMBER(10,2),
    P_CLOSE NUMBER(10,2),
    P_HIGH NUMBER(10,2),
    P_LOW NUMBER(10,2),
    VOLUME  NUMBER(30,0),
    PRIMARY KEY(ID)
);
